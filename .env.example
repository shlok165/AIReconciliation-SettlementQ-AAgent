POLLINATIONS_API_KEY=sk_replace_with_your_secret_key
# `openai-fast` is a good low-latency default.  Any model listed by Pollinations
# can be used here, for example `openai` or `claude-fast`.
POLLINATIONS_MODEL=openai-fast
